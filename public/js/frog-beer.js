// Generated by CoffeeScript 1.9.0
(function() {
  var highlightActiveColor, updateColor;

  console.log(colorsets);

  updateColor = function(context) {
    var color;
    color = $(context).data('color');
    return highlightActiveColor(context);
  };

  highlightActiveColor = function(context) {
    $('.color').removeClass('active');
    return $(context).addClass('active');
  };

  $('.color').not('.shuffle').click(function() {
    var context;
    context = this;
    return updateColor(context);
  });

  $(document).keypress(function(key) {
    var context;
    if (key.which === 49) {
      context = $('.color')[0];
      return updateColor(context);
    } else if (key.which === 50) {
      context = $('.color')[1];
      return updateColor(context);
    } else if (key.which === 51) {
      context = $('.color')[2];
      return updateColor(context);
    } else if (key.which === 52) {
      context = $('.color')[3];
      return updateColor(context);
    } else if (key.which === 53) {
      context = $('.color')[4];
      return updateColor(context);
    } else if (key.which === 54) {
      context = $('.color')[5];
      return updateColor(context);
    }
  });

  $('.shuffle').click(function() {});

}).call(this);
